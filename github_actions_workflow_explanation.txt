================================================================================
                    GITHUB ACTIONS WORKFLOW EXPLANATION
                        main.yaml File Breakdown
================================================================================

🚀 PROJECT: Smart Product Advisor - Customer Support Agent
📁 FILE: .github/workflows/main.yaml
🎯 PURPOSE: Automatically build and deploy Docker images to AWS ECR

================================================================================
                              HEADER SECTION
================================================================================

name: Build and push image to ECR

📝 EXPLANATION:
   • Names your workflow "Build and push image to ECR"
   • This is just a human-readable label that appears in GitHub's Actions tab
   • Helps you identify this workflow among others

================================================================================
                              TRIGGER SECTION  
================================================================================

on:
  push:
    branches:
      - main

📝 EXPLANATION:
   • Tells GitHub "run this workflow whenever someone pushes code to main branch"
   • Think of this as an automatic trigger
   • Every time you save changes to your main branch, this process starts
   • You can also trigger on pull requests, schedules, or manual triggers

================================================================================
                             JOB DEFINITION
================================================================================

jobs:
  build-and-push-ecr-image:
    name: Build and Push to Amazon ECR
    runs-on: ubuntu-latest

📝 EXPLANATION:
   • jobs: Defines what work to do
   • build-and-push-ecr-image: Internal name for this job
   • name: Human-readable description
   • runs-on: ubuntu-latest: Tells GitHub to use a fresh Ubuntu Linux computer

================================================================================
                            STEP 1: GET YOUR CODE
================================================================================

- name: Check out code
  uses: actions/checkout@v2

📝 EXPLANATION:
   • Downloads your project's code to the Ubuntu computer
   • Like saying "copy my project files to this computer so we can work with them"
   • @v2 specifies the version of the checkout action to use
   • Essential first step - you need your code before you can build it!

================================================================================
                           STEP 2: INSTALL TOOLS
================================================================================

- name: Install Utilities
  run: |
    sudo apt-get update
    sudo apt-get install -y jq unzip

📝 EXPLANATION:
   • Installs helpful tools on the Ubuntu machine
   • jq: Tool for processing JSON data (JavaScript Object Notation)
   • unzip: Tool for extracting compressed files
   • sudo: Run commands with administrator privileges
   • apt-get: Package manager for Ubuntu/Debian systems
   • -y flag: Automatically answer "yes" to installation prompts

================================================================================
                          STEP 3: CONNECT TO AWS
================================================================================

- name: Configure AWS credentials
  uses: aws-actions/configure-aws-credentials@v1
  with:
    aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
    aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    aws-session-token: ${{ secrets.AWS_SESSION_TOKEN }}
    aws-region: us-east-1

📝 EXPLANATION:
   • Logs into AWS using secret credentials stored in GitHub
   • Like entering your username/password to access Amazon's cloud services
   • secrets.: These are securely stored passwords that GitHub keeps hidden
   • AWS credentials include:
     - Access Key ID: Your AWS username
     - Secret Access Key: Your AWS password
     - Session Token: Temporary security token (if using temporary credentials)
   • us-east-1: AWS region where your resources are located

================================================================================
                      STEP 4: LOGIN TO DOCKER REGISTRY
================================================================================

- name: Login to Amazon ECR
  id: login-ecr
  uses: aws-actions/amazon-ecr-login@v1

📝 EXPLANATION:
   • Logs into Amazon ECR (Elastic Container Registry)
   • ECR is like a storage warehouse for Docker images on Amazon's cloud
   • id: login-ecr: Gives this step a name so other steps can reference results
   • This step outputs the registry URL that we'll use later

================================================================================
                         STEP 5: THE DOCKER MAGIC! 🐳
================================================================================

- name: Build, tag, and push image to Amazon ECR
  env:
    ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
    ECR_REPOSITORY: customer-support-agent
    IMAGE_TAG: latest
  run: |
    docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
    docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

📝 ENVIRONMENT VARIABLES SETUP:
   • ECR_REGISTRY: The address of your Docker storage warehouse (from login step)
   • ECR_REPOSITORY: The specific folder name (customer-support-agent)
   • IMAGE_TAG: Version label (latest means newest version)

📝 THE DOCKER COMMANDS:

   🔨 DOCKER BUILD COMMAND:
   docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .
   
   • docker build: Creates a Docker image from your code
   • -t: "Tag" - gives the image a name
   • The name becomes: [warehouse-address]/customer-support-agent:latest
   • . (dot): "Look in current directory for Dockerfile"
   
   📤 DOCKER PUSH COMMAND:
   docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
   
   • docker push: Uploads your Docker image to the cloud storage
   • Like uploading a file to Google Drive, but for Docker images
   • Makes your image available for deployment anywhere

================================================================================
                           WHAT IS DOCKER? 🐳
                            (Simple Explanation)
================================================================================

Think of Docker like this:

🍽️  Your app: A recipe for a meal
📦  Docker image: A pre-made frozen dinner with all ingredients
🔥  Docker container: Actually cooking and eating that frozen dinner  
🏪  ECR: A freezer warehouse that stores all your frozen dinners

Benefits:
• Your app will run the same way everywhere
• Easy to share with other developers
• Consistent environments across development, testing, and production
• Simplified deployment and scaling

================================================================================
                        WHAT THIS WORKFLOW DOES OVERALL
================================================================================

🎬 THE COMPLETE PROCESS:

1. 🚀 TRIGGER: You push code to GitHub main branch

2. 🖥️  SETUP: GitHub prepares a clean Ubuntu computer in the cloud

3. 📥 DOWNLOAD: Gets your latest code from the repository

4. 🛠️  PREPARE: Installs necessary tools and connects to AWS

5. 🔨 BUILD: Creates a "frozen dinner" (Docker image) from your Smart Product 
           Advisor app using your Dockerfile

6. 🏪 STORE: Uploads that "frozen dinner" to Amazon's warehouse (ECR)

7. ✅ RESULT: Now anyone can download and run your app from the cloud!

================================================================================
                              WHY THIS IS USEFUL
================================================================================

✨ CONSISTENCY: Your app runs exactly the same everywhere
🚀 DEPLOYMENT: Easy to deploy to production servers  
👥 COLLABORATION: Other developers can run your exact environment
📈 SCALING: Makes it much easier to scale your application
🔄 AUTOMATION: No manual deployment steps - everything is automated
🛡️  RELIABILITY: Reduces "it works on my machine" problems

================================================================================
                            IMPORTANT NOTES
================================================================================

🔐 SECURITY:
   • Never put AWS credentials directly in your code
   • Always use GitHub Secrets for sensitive information
   • ECR repository name can be different from GitHub repository name

🏗️  REQUIREMENTS:
   • You need a Dockerfile in your project root
   • AWS account with ECR repository created
   • GitHub repository with proper secrets configured

📝 CUSTOMIZATION:
   • You can change ECR_REPOSITORY name to anything you want
   • You can modify IMAGE_TAG for versioning (v1.0, v2.0, etc.)
   • You can add additional steps for testing, notifications, etc.

================================================================================
                               END OF EXPLANATION
================================================================================

📚 This workflow automates the entire process of taking your Smart Product 
   Advisor code and making it ready for deployment in the cloud!

🎯 Next steps: Make sure you have a Dockerfile and AWS ECR repository set up
   to complete your deployment pipeline. 